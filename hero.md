# hero.md

Read the existing Next.js project first.

Task:
- Create a second **static hero section** for JKD International Inter College.
- Use the provided premium front-view image as the background.
- Keep the current hero as the **dynamic hero**.
- Add an admin panel toggle: `Hero Mode = Dynamic | Static`.
- Save the selected mode in the existing settings/store/database used by the project.
- Render:
  - Dynamic → existing hero.
  - Static → new premium hero.
- Do not break any existing functionality.
- Reuse existing components, styling, and architecture as much as possible.
- After implementation, update the project progress/status file with this feature:
  `Added Hero Mode toggle (Dynamic/Static) with admin-controlled rendering.`